spring:
  application:
    name: bookservice
---
spring:
  profiles: default
  cloud:
    config:
      uri: http://localhost:8888
---
spring:
  profiles: prod
  cloud:
    config:
      uri: http://localhost:8111